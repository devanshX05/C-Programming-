#include<stdio.h>
typedef float realNumber;// now we can realNumber instead of flaot

int main(){
    int x=5;
    // float y; //if i am writing type def of float then we can realnumber instead of float in all code 
    realNumber y=3.14;
    printf("%f",y);
    return 0;
}