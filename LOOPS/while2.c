#include<stdio.h>
int main (){
    int i=10;
    while(i==20) //main funtions are not defined (no termination condition is defined)
    printf("\n malayalam is a palitrome"); //it is an infinite loop
    return 0;
}